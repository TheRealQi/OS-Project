#include "types.h"
#include "stat.h"
#include "user.h"
#include "pstat.h"

int main(int argc, char* argv[]){
    int N = atoi(argv[1]);
    //int proc = atoi(argv[2]);
    settickets(N);
    //printf(1, "Set process tickets to %d", N);
    while(1){
        //printf(1, "\nProcess Proc %d is running... with PID: ", proc);
        continue;
    }
    exit();
}
